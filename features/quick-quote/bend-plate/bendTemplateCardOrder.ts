import type { BendTemplateId } from "./types";

/** Grid order for template cards (shape picker / plate project) — right column first. */
export const BEND_TEMPLATE_CARD_ORDER: BendTemplateId[] = [
  "u",
  "l",
  "omega",
  "z",
  "custom",
  "gutter",
  "plate",
];
