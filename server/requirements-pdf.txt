# Python dependencies for quotation PDF export (Playwright + Jinja2).
# Install: pip install -r server/requirements-pdf.txt
# Then:    playwright install chromium

playwright>=1.49.0
jinja2>=3.1.4
pydantic>=2.10.0
